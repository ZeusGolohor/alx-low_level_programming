#include "main.h"
#include <string.h>

/**
 * _strcat - Used to combine
 * two strings together
 * @dest: first operand
 * @src: second operand
 * Return: char *
 */
char *_strcat(char *dest, char *src)
{
	return (strcat(dest, src));
}
