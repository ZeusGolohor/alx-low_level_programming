#include "main.h"
#include <string.h>

/**
 * _strcmp - Used to check equality of
 * of two strings
 * @s1: first operand.
 * @s2: second operand.
 * Return: Always an int.
 */

int _strcmp(char *s1, char *s2)
{
	return (strcmp(s1, s2));
}
