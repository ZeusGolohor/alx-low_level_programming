#include "main.h"
#include <string.h>

/**
 * _strlen - To get the len.
 * of a string.
 * @s: first operand
 * Return: Always an int.
 */

int _strlen(char *s)
{
	int len;

	len = strlen(s);
	return (len);
}
